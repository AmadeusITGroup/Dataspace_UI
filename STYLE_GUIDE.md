### Checklist for WebStorm:

Install and enable ESLint and Stylelint plugins.
Set up auto-format on save for TypeScript/JavaScript/SCSS.
Run lint checks before committing code.
Ensure code passes all style and lint checks (no errors/warnings).

### Code formatting configuration:

Use the rules defined in eslint.config.mjs and stylelint.config.mjs.
